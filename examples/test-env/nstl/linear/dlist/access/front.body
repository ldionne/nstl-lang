#if CONCAT(begin_, NSTL_PACKAGE, _, NSTL_DEPTH, _H) != 1
    #include "begin.contents"
#endif

                static inline ValueType_ FuncName_(front) (Container_ *list)
                {
                	return *FuncName_(begin)(list);
                }
            
