#if CONCAT(Iterator_, NSTL_PACKAGE, _, NSTL_DEPTH, _H) != 1
    #include "Iterator.contents"
#endif
#if CONCAT(prev_, NSTL_PACKAGE, _, NSTL_DEPTH, _H) != 1
    #include "prev.contents"
#endif
#if CONCAT(end_, NSTL_PACKAGE, _, NSTL_DEPTH, _H) != 1
    #include "end.contents"
#endif

                static inline Iterator_ FuncName_(rbegin) (Container_ *list)
                {
                	return FuncName_(prev)(FuncName_(end)(list));
                }
            
